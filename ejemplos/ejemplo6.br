declare foo, bar :: integer execute
    bar := 3 ;
    $- Asignar a foo
    el valor 35 + bar, si bar > 2. -$
    declare foo :: boolean execute
        if bar > 2 then
            foo := false
        done
    done
done
